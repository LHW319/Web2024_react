import { useLoaderData } from 'react-router-dom'

export default function DashboardPage() {
  //const user = useLoaderData() => loader 함수에서 리턴한 데이터를 받아온다!

  return (
    <>
      <h1>Dashboard Page!</h1>
    </>
  )
}
